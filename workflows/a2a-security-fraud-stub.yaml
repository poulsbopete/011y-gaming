name: Aether Games A2A Security fraud stub
description: >
  Stub workflow documenting A2A federation from Observability to Security for
  account fraud correlation. Instruqt cannot stand up a second Serverless
  project — this workflow is a documentation / preview artifact. Production
  would call a Security agent endpoint.
enabled: false
tags: [aether-games, a2a, stub, security]
triggers:
  - type: manual
steps:
  - name: explain_stub
    type: console
    with:
      message: >
        STUB ONLY — Observability Serverless is the only project in this play.
        Canned fraud correlation lives at build/a2a-stub/security-fraud-correlation.json.
        Live Security: https://my-security-project-ac9463.kb.us-central1.gcp.elastic.cloud
  - name: note_production_a2a
    type: console
    with:
      message: >
        Production pattern: Obs ai.agent → Security fraud-correlator agent (A2A)
        with scoped API key; open Security case linked to auth/anticheat signals.
